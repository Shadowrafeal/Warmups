#include<iostream>
using namespace std;
int main() {
	int width;
	int length;
	int area;
	cout << "what is the width and length";
	cin >> width;
	cin >> length;
	area = width * length;
	cout << "the area is" << area << endl;
}
